/* Each view uses the same renderer. A render manages the...
    WebGL Context
    The 3D view (VolumeRenderer)
    The sphere widget (SphereRenderer)
    The slicer widget (SlicerRenderer)
*/
class MasterRenderer() {
    constructor(id) {

    }
}
